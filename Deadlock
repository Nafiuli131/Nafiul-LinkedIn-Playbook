🔒Deadlock — যখন সবাই অপেক্ষায়, কেউই এগোয় না

ধরুন, দুইজন বন্ধু আছে — রাহিম আর করিম।
রাহিমের কাছে আছে একটা pen ✏️
করিমের কাছে আছে একটা notebook 📒।
রাহিম বলল, “তুমি notebook দাও, আমি pen দিয়ে লিখব।”
করিম বলল, “তুমি pen দাও, আমি notebook দিয়ে লিখব।”
এখন দু’জনেই একে অপরের জন্য অপেক্ষা করছে…
আর কেউই লিখতে পারছে না 😅
এটাই 👉 Deadlock

💡 In tech terms:
 যখন দুইটা thread একে অপরের resource ধরে রাখে
 এবং কেউই release করে না,
 তখন সিস্টেম আটকে যায় — not crashed, just frozen 🧊

🧠 Classic example:
 Thread A locks Table-1 → waits for Table-2
 Thread B locks Table-2 → waits for Table-1
 => Welcome to infinite waiting room ⏳

🧩 How to avoid it:
Always lock resources in a consistent order
Use timeout for acquiring locks
Apply Deadlock detection algorithms

💬 Fun fact:
 Deadlock মানে system মারা যায় না,
 শুধু “সাইলেন্ট ধর্মঘট”-এ বসে যায় 😅
আপনার কি কখনো এমন হয়েছে — code চলতেছে না, কিন্তু crash-ও করছে না?
 That’s probably your threads having tea break together! 
